---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: lynqforms.operator.lynq.sh
spec:
  group: operator.lynq.sh
  names:
    kind: LynqForm
    listKind: LynqFormList
    plural: lynqforms
    singular: lynqform
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: LynqHub reference
      jsonPath: .spec.hubId
      name: Hub
      type: string
    - description: Total nodes using form
      jsonPath: .status.totalNodes
      name: Total
      type: integer
    - description: Ready nodes
      jsonPath: .status.readyNodes
      name: Ready
      type: integer
    - description: Rollout phase
      jsonPath: .status.rollout.phase
      name: Rollout
      type: string
    - description: Nodes currently updating
      jsonPath: .status.rollout.updatingNodes
      name: Updating
      type: integer
    - description: Applied status
      jsonPath: .status.conditions[?(@.type=='Applied')].status
      name: Applied
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: LynqForm is the Schema for the lynqforms API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              LynqFormSpec defines the desired state of LynqForm.
              Resources are created in the same namespace as the LynqNode CR by default.
              Use TResource.targetNamespace to create resources in different namespaces.
              Namespaces can be created using the dedicated 'namespaces' field or 'manifests' field.
            properties:
              configMaps:
                description: ConfigMaps defines ConfigMap resources to create
                items:
                  description: TResource defines a Kubernetes resource template with
                    policies and dependencies
                  properties:
                    annotationsTemplate:
                      additionalProperties:
                        type: string
                      description: AnnotationsTemplate defines annotations to apply
                        to the resource (supports templates)
                      type: object
                    conflictPolicy:
                      default: Stuck
                      description: |-
                        ConflictPolicy determines how to handle conflicts with existing resources
                        Default: Stuck (fail reconciliation if resource exists with different owner)
                      enum:
                      - Force
                      - Stuck
                      type: string
                    creationPolicy:
                      default: WhenNeeded
                      description: |-
                        CreationPolicy determines when the resource should be created
                        Default: WhenNeeded
                      enum:
                      - Once
                      - WhenNeeded
                      type: string
                    deletionPolicy:
                      default: Delete
                      description: |-
                        DeletionPolicy determines what happens to the resource when the LynqNode is deleted
                        Default: Delete
                      enum:
                      - Delete
                      - Retain
                      type: string
                    dependIds:
                      description: DependIds lists IDs of resources that must be ready
                        before this resource is created
                      items:
                        type: string
                      type: array
                    id:
                      description: ID is a unique identifier within the template (used
                        for dependencies and references)
                      type: string
                    ignoreFields:
                      description: |-
                        IgnoreFields specifies JSONPath expressions for fields to exclude from synchronization
                        These fields will be applied during initial creation but ignored in subsequent reconciliations
                        Only effective when CreationPolicy is WhenNeeded (default)
                        Allows fine-grained control for fields that should be managed externally (e.g., HPA-controlled replicas)
                        Example: ["$.spec.replicas", "$.spec.template.spec.containers[0].resources"]
                      items:
                        type: string
                      type: array
                    labelsTemplate:
                      additionalProperties:
                        type: string
                      description: LabelsTemplate defines labels to apply to the resource
                        (supports templates)
                      type: object
                    nameTemplate:
                      description: |-
                        NameTemplate is a Go template for the resource name
                        Template variables: .uid, .host, .hostOrUrl, and extraValueMappings
                      type: string
                    patchStrategy:
                      default: apply
                      description: |-
                        PatchStrategy determines how to apply the resource
                        Default: apply (Server-Side Apply)
                      enum:
                      - apply
                      - merge
                      - replace
                      type: string
                    skipOnDependencyFailure:
                      default: true
                      description: |-
                        SkipOnDependencyFailure determines whether to skip creating this resource when a dependency fails
                        When true (default): This resource will be skipped if any of its dependencies fail
                        When false: This resource will still be created even if dependencies fail (useful for cleanup resources)
                        Default: true
                      type: boolean
                    spec:
                      description: |-
                        Spec is the Kubernetes resource specification
                        Can be any Kubernetes native resource or custom resource
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    targetNamespace:
                      description: |-
                        TargetNamespace specifies the namespace where the resource should be created
                        If empty, defaults to the same namespace as the LynqNode CR
                        For cross-namespace resources, label-based tracking is used instead of ownerReferences
                        Supports Go template syntax (e.g., "{{ .uid }}-namespace")
                      type: string
                    timeoutSeconds:
                      default: 300
                      description: |-
                        TimeoutSeconds is the maximum time to wait for the resource to be ready
                        Default: 300
                      format: int32
                      maximum: 3600
                      minimum: 1
                      type: integer
                    waitForReady:
                      default: true
                      description: |-
                        WaitForReady determines whether to wait for the resource to be ready before continuing
                        Default: true
                      type: boolean
                  required:
                  - id
                  - spec
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
              cronJobs:
                description: CronJobs defines CronJob resources to create
                items:
                  description: TResource defines a Kubernetes resource template with
                    policies and dependencies
                  properties:
                    annotationsTemplate:
                      additionalProperties:
                        type: string
                      description: AnnotationsTemplate defines annotations to apply
                        to the resource (supports templates)
                      type: object
                    conflictPolicy:
                      default: Stuck
                      description: |-
                        ConflictPolicy determines how to handle conflicts with existing resources
                        Default: Stuck (fail reconciliation if resource exists with different owner)
                      enum:
                      - Force
                      - Stuck
                      type: string
                    creationPolicy:
                      default: WhenNeeded
                      description: |-
                        CreationPolicy determines when the resource should be created
                        Default: WhenNeeded
                      enum:
                      - Once
                      - WhenNeeded
                      type: string
                    deletionPolicy:
                      default: Delete
                      description: |-
                        DeletionPolicy determines what happens to the resource when the LynqNode is deleted
                        Default: Delete
                      enum:
                      - Delete
                      - Retain
                      type: string
                    dependIds:
                      description: DependIds lists IDs of resources that must be ready
                        before this resource is created
                      items:
                        type: string
                      type: array
                    id:
                      description: ID is a unique identifier within the template (used
                        for dependencies and references)
                      type: string
                    ignoreFields:
                      description: |-
                        IgnoreFields specifies JSONPath expressions for fields to exclude from synchronization
                        These fields will be applied during initial creation but ignored in subsequent reconciliations
                        Only effective when CreationPolicy is WhenNeeded (default)
                        Allows fine-grained control for fields that should be managed externally (e.g., HPA-controlled replicas)
                        Example: ["$.spec.replicas", "$.spec.template.spec.containers[0].resources"]
                      items:
                        type: string
                      type: array
                    labelsTemplate:
                      additionalProperties:
                        type: string
                      description: LabelsTemplate defines labels to apply to the resource
                        (supports templates)
                      type: object
                    nameTemplate:
                      description: |-
                        NameTemplate is a Go template for the resource name
                        Template variables: .uid, .host, .hostOrUrl, and extraValueMappings
                      type: string
                    patchStrategy:
                      default: apply
                      description: |-
                        PatchStrategy determines how to apply the resource
                        Default: apply (Server-Side Apply)
                      enum:
                      - apply
                      - merge
                      - replace
                      type: string
                    skipOnDependencyFailure:
                      default: true
                      description: |-
                        SkipOnDependencyFailure determines whether to skip creating this resource when a dependency fails
                        When true (default): This resource will be skipped if any of its dependencies fail
                        When false: This resource will still be created even if dependencies fail (useful for cleanup resources)
                        Default: true
                      type: boolean
                    spec:
                      description: |-
                        Spec is the Kubernetes resource specification
                        Can be any Kubernetes native resource or custom resource
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    targetNamespace:
                      description: |-
                        TargetNamespace specifies the namespace where the resource should be created
                        If empty, defaults to the same namespace as the LynqNode CR
                        For cross-namespace resources, label-based tracking is used instead of ownerReferences
                        Supports Go template syntax (e.g., "{{ .uid }}-namespace")
                      type: string
                    timeoutSeconds:
                      default: 300
                      description: |-
                        TimeoutSeconds is the maximum time to wait for the resource to be ready
                        Default: 300
                      format: int32
                      maximum: 3600
                      minimum: 1
                      type: integer
                    waitForReady:
                      default: true
                      description: |-
                        WaitForReady determines whether to wait for the resource to be ready before continuing
                        Default: true
                      type: boolean
                  required:
                  - id
                  - spec
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
              daemonSets:
                description: DaemonSets defines DaemonSet resources to create
                items:
                  description: TResource defines a Kubernetes resource template with
                    policies and dependencies
                  properties:
                    annotationsTemplate:
                      additionalProperties:
                        type: string
                      description: AnnotationsTemplate defines annotations to apply
                        to the resource (supports templates)
                      type: object
                    conflictPolicy:
                      default: Stuck
                      description: |-
                        ConflictPolicy determines how to handle conflicts with existing resources
                        Default: Stuck (fail reconciliation if resource exists with different owner)
                      enum:
                      - Force
                      - Stuck
                      type: string
                    creationPolicy:
                      default: WhenNeeded
                      description: |-
                        CreationPolicy determines when the resource should be created
                        Default: WhenNeeded
                      enum:
                      - Once
                      - WhenNeeded
                      type: string
                    deletionPolicy:
                      default: Delete
                      description: |-
                        DeletionPolicy determines what happens to the resource when the LynqNode is deleted
                        Default: Delete
                      enum:
                      - Delete
                      - Retain
                      type: string
                    dependIds:
                      description: DependIds lists IDs of resources that must be ready
                        before this resource is created
                      items:
                        type: string
                      type: array
                    id:
                      description: ID is a unique identifier within the template (used
                        for dependencies and references)
                      type: string
                    ignoreFields:
                      description: |-
                        IgnoreFields specifies JSONPath expressions for fields to exclude from synchronization
                        These fields will be applied during initial creation but ignored in subsequent reconciliations
                        Only effective when CreationPolicy is WhenNeeded (default)
                        Allows fine-grained control for fields that should be managed externally (e.g., HPA-controlled replicas)
                        Example: ["$.spec.replicas", "$.spec.template.spec.containers[0].resources"]
                      items:
                        type: string
                      type: array
                    labelsTemplate:
                      additionalProperties:
                        type: string
                      description: LabelsTemplate defines labels to apply to the resource
                        (supports templates)
                      type: object
                    nameTemplate:
                      description: |-
                        NameTemplate is a Go template for the resource name
                        Template variables: .uid, .host, .hostOrUrl, and extraValueMappings
                      type: string
                    patchStrategy:
                      default: apply
                      description: |-
                        PatchStrategy determines how to apply the resource
                        Default: apply (Server-Side Apply)
                      enum:
                      - apply
                      - merge
                      - replace
                      type: string
                    skipOnDependencyFailure:
                      default: true
                      description: |-
                        SkipOnDependencyFailure determines whether to skip creating this resource when a dependency fails
                        When true (default): This resource will be skipped if any of its dependencies fail
                        When false: This resource will still be created even if dependencies fail (useful for cleanup resources)
                        Default: true
                      type: boolean
                    spec:
                      description: |-
                        Spec is the Kubernetes resource specification
                        Can be any Kubernetes native resource or custom resource
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    targetNamespace:
                      description: |-
                        TargetNamespace specifies the namespace where the resource should be created
                        If empty, defaults to the same namespace as the LynqNode CR
                        For cross-namespace resources, label-based tracking is used instead of ownerReferences
                        Supports Go template syntax (e.g., "{{ .uid }}-namespace")
                      type: string
                    timeoutSeconds:
                      default: 300
                      description: |-
                        TimeoutSeconds is the maximum time to wait for the resource to be ready
                        Default: 300
                      format: int32
                      maximum: 3600
                      minimum: 1
                      type: integer
                    waitForReady:
                      default: true
                      description: |-
                        WaitForReady determines whether to wait for the resource to be ready before continuing
                        Default: true
                      type: boolean
                  required:
                  - id
                  - spec
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
              deployments:
                description: Deployments defines Deployment resources to create
                items:
                  description: TResource defines a Kubernetes resource template with
                    policies and dependencies
                  properties:
                    annotationsTemplate:
                      additionalProperties:
                        type: string
                      description: AnnotationsTemplate defines annotations to apply
                        to the resource (supports templates)
                      type: object
                    conflictPolicy:
                      default: Stuck
                      description: |-
                        ConflictPolicy determines how to handle conflicts with existing resources
                        Default: Stuck (fail reconciliation if resource exists with different owner)
                      enum:
                      - Force
                      - Stuck
                      type: string
                    creationPolicy:
                      default: WhenNeeded
                      description: |-
                        CreationPolicy determines when the resource should be created
                        Default: WhenNeeded
                      enum:
                      - Once
                      - WhenNeeded
                      type: string
                    deletionPolicy:
                      default: Delete
                      description: |-
                        DeletionPolicy determines what happens to the resource when the LynqNode is deleted
                        Default: Delete
                      enum:
                      - Delete
                      - Retain
                      type: string
                    dependIds:
                      description: DependIds lists IDs of resources that must be ready
                        before this resource is created
                      items:
                        type: string
                      type: array
                    id:
                      description: ID is a unique identifier within the template (used
                        for dependencies and references)
                      type: string
                    ignoreFields:
                      description: |-
                        IgnoreFields specifies JSONPath expressions for fields to exclude from synchronization
                        These fields will be applied during initial creation but ignored in subsequent reconciliations
                        Only effective when CreationPolicy is WhenNeeded (default)
                        Allows fine-grained control for fields that should be managed externally (e.g., HPA-controlled replicas)
                        Example: ["$.spec.replicas", "$.spec.template.spec.containers[0].resources"]
                      items:
                        type: string
                      type: array
                    labelsTemplate:
                      additionalProperties:
                        type: string
                      description: LabelsTemplate defines labels to apply to the resource
                        (supports templates)
                      type: object
                    nameTemplate:
                      description: |-
                        NameTemplate is a Go template for the resource name
                        Template variables: .uid, .host, .hostOrUrl, and extraValueMappings
                      type: string
                    patchStrategy:
                      default: apply
                      description: |-
                        PatchStrategy determines how to apply the resource
                        Default: apply (Server-Side Apply)
                      enum:
                      - apply
                      - merge
                      - replace
                      type: string
                    skipOnDependencyFailure:
                      default: true
                      description: |-
                        SkipOnDependencyFailure determines whether to skip creating this resource when a dependency fails
                        When true (default): This resource will be skipped if any of its dependencies fail
                        When false: This resource will still be created even if dependencies fail (useful for cleanup resources)
                        Default: true
                      type: boolean
                    spec:
                      description: |-
                        Spec is the Kubernetes resource specification
                        Can be any Kubernetes native resource or custom resource
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    targetNamespace:
                      description: |-
                        TargetNamespace specifies the namespace where the resource should be created
                        If empty, defaults to the same namespace as the LynqNode CR
                        For cross-namespace resources, label-based tracking is used instead of ownerReferences
                        Supports Go template syntax (e.g., "{{ .uid }}-namespace")
                      type: string
                    timeoutSeconds:
                      default: 300
                      description: |-
                        TimeoutSeconds is the maximum time to wait for the resource to be ready
                        Default: 300
                      format: int32
                      maximum: 3600
                      minimum: 1
                      type: integer
                    waitForReady:
                      default: true
                      description: |-
                        WaitForReady determines whether to wait for the resource to be ready before continuing
                        Default: true
                      type: boolean
                  required:
                  - id
                  - spec
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
              horizontalPodAutoscalers:
                description: HorizontalPodAutoscalers defines HPA resources to create
                items:
                  description: TResource defines a Kubernetes resource template with
                    policies and dependencies
                  properties:
                    annotationsTemplate:
                      additionalProperties:
                        type: string
                      description: AnnotationsTemplate defines annotations to apply
                        to the resource (supports templates)
                      type: object
                    conflictPolicy:
                      default: Stuck
                      description: |-
                        ConflictPolicy determines how to handle conflicts with existing resources
                        Default: Stuck (fail reconciliation if resource exists with different owner)
                      enum:
                      - Force
                      - Stuck
                      type: string
                    creationPolicy:
                      default: WhenNeeded
                      description: |-
                        CreationPolicy determines when the resource should be created
                        Default: WhenNeeded
                      enum:
                      - Once
                      - WhenNeeded
                      type: string
                    deletionPolicy:
                      default: Delete
                      description: |-
                        DeletionPolicy determines what happens to the resource when the LynqNode is deleted
                        Default: Delete
                      enum:
                      - Delete
                      - Retain
                      type: string
                    dependIds:
                      description: DependIds lists IDs of resources that must be ready
                        before this resource is created
                      items:
                        type: string
                      type: array
                    id:
                      description: ID is a unique identifier within the template (used
                        for dependencies and references)
                      type: string
                    ignoreFields:
                      description: |-
                        IgnoreFields specifies JSONPath expressions for fields to exclude from synchronization
                        These fields will be applied during initial creation but ignored in subsequent reconciliations
                        Only effective when CreationPolicy is WhenNeeded (default)
                        Allows fine-grained control for fields that should be managed externally (e.g., HPA-controlled replicas)
                        Example: ["$.spec.replicas", "$.spec.template.spec.containers[0].resources"]
                      items:
                        type: string
                      type: array
                    labelsTemplate:
                      additionalProperties:
                        type: string
                      description: LabelsTemplate defines labels to apply to the resource
                        (supports templates)
                      type: object
                    nameTemplate:
                      description: |-
                        NameTemplate is a Go template for the resource name
                        Template variables: .uid, .host, .hostOrUrl, and extraValueMappings
                      type: string
                    patchStrategy:
                      default: apply
                      description: |-
                        PatchStrategy determines how to apply the resource
                        Default: apply (Server-Side Apply)
                      enum:
                      - apply
                      - merge
                      - replace
                      type: string
                    skipOnDependencyFailure:
                      default: true
                      description: |-
                        SkipOnDependencyFailure determines whether to skip creating this resource when a dependency fails
                        When true (default): This resource will be skipped if any of its dependencies fail
                        When false: This resource will still be created even if dependencies fail (useful for cleanup resources)
                        Default: true
                      type: boolean
                    spec:
                      description: |-
                        Spec is the Kubernetes resource specification
                        Can be any Kubernetes native resource or custom resource
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    targetNamespace:
                      description: |-
                        TargetNamespace specifies the namespace where the resource should be created
                        If empty, defaults to the same namespace as the LynqNode CR
                        For cross-namespace resources, label-based tracking is used instead of ownerReferences
                        Supports Go template syntax (e.g., "{{ .uid }}-namespace")
                      type: string
                    timeoutSeconds:
                      default: 300
                      description: |-
                        TimeoutSeconds is the maximum time to wait for the resource to be ready
                        Default: 300
                      format: int32
                      maximum: 3600
                      minimum: 1
                      type: integer
                    waitForReady:
                      default: true
                      description: |-
                        WaitForReady determines whether to wait for the resource to be ready before continuing
                        Default: true
                      type: boolean
                  required:
                  - id
                  - spec
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
              hubId:
                description: HubID references the LynqHub that this form is associated
                  with
                type: string
                x-kubernetes-validations:
                - message: hubId is immutable
                  rule: self == oldSelf
              ingresses:
                description: Ingresses defines Ingress resources to create
                items:
                  description: TResource defines a Kubernetes resource template with
                    policies and dependencies
                  properties:
                    annotationsTemplate:
                      additionalProperties:
                        type: string
                      description: AnnotationsTemplate defines annotations to apply
                        to the resource (supports templates)
                      type: object
                    conflictPolicy:
                      default: Stuck
                      description: |-
                        ConflictPolicy determines how to handle conflicts with existing resources
                        Default: Stuck (fail reconciliation if resource exists with different owner)
                      enum:
                      - Force
                      - Stuck
                      type: string
                    creationPolicy:
                      default: WhenNeeded
                      description: |-
                        CreationPolicy determines when the resource should be created
                        Default: WhenNeeded
                      enum:
                      - Once
                      - WhenNeeded
                      type: string
                    deletionPolicy:
                      default: Delete
                      description: |-
                        DeletionPolicy determines what happens to the resource when the LynqNode is deleted
                        Default: Delete
                      enum:
                      - Delete
                      - Retain
                      type: string
                    dependIds:
                      description: DependIds lists IDs of resources that must be ready
                        before this resource is created
                      items:
                        type: string
                      type: array
                    id:
                      description: ID is a unique identifier within the template (used
                        for dependencies and references)
                      type: string
                    ignoreFields:
                      description: |-
                        IgnoreFields specifies JSONPath expressions for fields to exclude from synchronization
                        These fields will be applied during initial creation but ignored in subsequent reconciliations
                        Only effective when CreationPolicy is WhenNeeded (default)
                        Allows fine-grained control for fields that should be managed externally (e.g., HPA-controlled replicas)
                        Example: ["$.spec.replicas", "$.spec.template.spec.containers[0].resources"]
                      items:
                        type: string
                      type: array
                    labelsTemplate:
                      additionalProperties:
                        type: string
                      description: LabelsTemplate defines labels to apply to the resource
                        (supports templates)
                      type: object
                    nameTemplate:
                      description: |-
                        NameTemplate is a Go template for the resource name
                        Template variables: .uid, .host, .hostOrUrl, and extraValueMappings
                      type: string
                    patchStrategy:
                      default: apply
                      description: |-
                        PatchStrategy determines how to apply the resource
                        Default: apply (Server-Side Apply)
                      enum:
                      - apply
                      - merge
                      - replace
                      type: string
                    skipOnDependencyFailure:
                      default: true
                      description: |-
                        SkipOnDependencyFailure determines whether to skip creating this resource when a dependency fails
                        When true (default): This resource will be skipped if any of its dependencies fail
                        When false: This resource will still be created even if dependencies fail (useful for cleanup resources)
                        Default: true
                      type: boolean
                    spec:
                      description: |-
                        Spec is the Kubernetes resource specification
                        Can be any Kubernetes native resource or custom resource
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    targetNamespace:
                      description: |-
                        TargetNamespace specifies the namespace where the resource should be created
                        If empty, defaults to the same namespace as the LynqNode CR
                        For cross-namespace resources, label-based tracking is used instead of ownerReferences
                        Supports Go template syntax (e.g., "{{ .uid }}-namespace")
                      type: string
                    timeoutSeconds:
                      default: 300
                      description: |-
                        TimeoutSeconds is the maximum time to wait for the resource to be ready
                        Default: 300
                      format: int32
                      maximum: 3600
                      minimum: 1
                      type: integer
                    waitForReady:
                      default: true
                      description: |-
                        WaitForReady determines whether to wait for the resource to be ready before continuing
                        Default: true
                      type: boolean
                  required:
                  - id
                  - spec
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
              jobs:
                description: Jobs defines Job resources to create
                items:
                  description: TResource defines a Kubernetes resource template with
                    policies and dependencies
                  properties:
                    annotationsTemplate:
                      additionalProperties:
                        type: string
                      description: AnnotationsTemplate defines annotations to apply
                        to the resource (supports templates)
                      type: object
                    conflictPolicy:
                      default: Stuck
                      description: |-
                        ConflictPolicy determines how to handle conflicts with existing resources
                        Default: Stuck (fail reconciliation if resource exists with different owner)
                      enum:
                      - Force
                      - Stuck
                      type: string
                    creationPolicy:
                      default: WhenNeeded
                      description: |-
                        CreationPolicy determines when the resource should be created
                        Default: WhenNeeded
                      enum:
                      - Once
                      - WhenNeeded
                      type: string
                    deletionPolicy:
                      default: Delete
                      description: |-
                        DeletionPolicy determines what happens to the resource when the LynqNode is deleted
                        Default: Delete
                      enum:
                      - Delete
                      - Retain
                      type: string
                    dependIds:
                      description: DependIds lists IDs of resources that must be ready
                        before this resource is created
                      items:
                        type: string
                      type: array
                    id:
                      description: ID is a unique identifier within the template (used
                        for dependencies and references)
                      type: string
                    ignoreFields:
                      description: |-
                        IgnoreFields specifies JSONPath expressions for fields to exclude from synchronization
                        These fields will be applied during initial creation but ignored in subsequent reconciliations
                        Only effective when CreationPolicy is WhenNeeded (default)
                        Allows fine-grained control for fields that should be managed externally (e.g., HPA-controlled replicas)
                        Example: ["$.spec.replicas", "$.spec.template.spec.containers[0].resources"]
                      items:
                        type: string
                      type: array
                    labelsTemplate:
                      additionalProperties:
                        type: string
                      description: LabelsTemplate defines labels to apply to the resource
                        (supports templates)
                      type: object
                    nameTemplate:
                      description: |-
                        NameTemplate is a Go template for the resource name
                        Template variables: .uid, .host, .hostOrUrl, and extraValueMappings
                      type: string
                    patchStrategy:
                      default: apply
                      description: |-
                        PatchStrategy determines how to apply the resource
                        Default: apply (Server-Side Apply)
                      enum:
                      - apply
                      - merge
                      - replace
                      type: string
                    skipOnDependencyFailure:
                      default: true
                      description: |-
                        SkipOnDependencyFailure determines whether to skip creating this resource when a dependency fails
                        When true (default): This resource will be skipped if any of its dependencies fail
                        When false: This resource will still be created even if dependencies fail (useful for cleanup resources)
                        Default: true
                      type: boolean
                    spec:
                      description: |-
                        Spec is the Kubernetes resource specification
                        Can be any Kubernetes native resource or custom resource
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    targetNamespace:
                      description: |-
                        TargetNamespace specifies the namespace where the resource should be created
                        If empty, defaults to the same namespace as the LynqNode CR
                        For cross-namespace resources, label-based tracking is used instead of ownerReferences
                        Supports Go template syntax (e.g., "{{ .uid }}-namespace")
                      type: string
                    timeoutSeconds:
                      default: 300
                      description: |-
                        TimeoutSeconds is the maximum time to wait for the resource to be ready
                        Default: 300
                      format: int32
                      maximum: 3600
                      minimum: 1
                      type: integer
                    waitForReady:
                      default: true
                      description: |-
                        WaitForReady determines whether to wait for the resource to be ready before continuing
                        Default: true
                      type: boolean
                  required:
                  - id
                  - spec
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
              manifests:
                description: |-
                  Manifests defines arbitrary Kubernetes resources as raw manifests
                  Use this for any resource type not explicitly supported above
                items:
                  description: TResource defines a Kubernetes resource template with
                    policies and dependencies
                  properties:
                    annotationsTemplate:
                      additionalProperties:
                        type: string
                      description: AnnotationsTemplate defines annotations to apply
                        to the resource (supports templates)
                      type: object
                    conflictPolicy:
                      default: Stuck
                      description: |-
                        ConflictPolicy determines how to handle conflicts with existing resources
                        Default: Stuck (fail reconciliation if resource exists with different owner)
                      enum:
                      - Force
                      - Stuck
                      type: string
                    creationPolicy:
                      default: WhenNeeded
                      description: |-
                        CreationPolicy determines when the resource should be created
                        Default: WhenNeeded
                      enum:
                      - Once
                      - WhenNeeded
                      type: string
                    deletionPolicy:
                      default: Delete
                      description: |-
                        DeletionPolicy determines what happens to the resource when the LynqNode is deleted
                        Default: Delete
                      enum:
                      - Delete
                      - Retain
                      type: string
                    dependIds:
                      description: DependIds lists IDs of resources that must be ready
                        before this resource is created
                      items:
                        type: string
                      type: array
                    id:
                      description: ID is a unique identifier within the template (used
                        for dependencies and references)
                      type: string
                    ignoreFields:
                      description: |-
                        IgnoreFields specifies JSONPath expressions for fields to exclude from synchronization
                        These fields will be applied during initial creation but ignored in subsequent reconciliations
                        Only effective when CreationPolicy is WhenNeeded (default)
                        Allows fine-grained control for fields that should be managed externally (e.g., HPA-controlled replicas)
                        Example: ["$.spec.replicas", "$.spec.template.spec.containers[0].resources"]
                      items:
                        type: string
                      type: array
                    labelsTemplate:
                      additionalProperties:
                        type: string
                      description: LabelsTemplate defines labels to apply to the resource
                        (supports templates)
                      type: object
                    nameTemplate:
                      description: |-
                        NameTemplate is a Go template for the resource name
                        Template variables: .uid, .host, .hostOrUrl, and extraValueMappings
                      type: string
                    patchStrategy:
                      default: apply
                      description: |-
                        PatchStrategy determines how to apply the resource
                        Default: apply (Server-Side Apply)
                      enum:
                      - apply
                      - merge
                      - replace
                      type: string
                    skipOnDependencyFailure:
                      default: true
                      description: |-
                        SkipOnDependencyFailure determines whether to skip creating this resource when a dependency fails
                        When true (default): This resource will be skipped if any of its dependencies fail
                        When false: This resource will still be created even if dependencies fail (useful for cleanup resources)
                        Default: true
                      type: boolean
                    spec:
                      description: |-
                        Spec is the Kubernetes resource specification
                        Can be any Kubernetes native resource or custom resource
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    targetNamespace:
                      description: |-
                        TargetNamespace specifies the namespace where the resource should be created
                        If empty, defaults to the same namespace as the LynqNode CR
                        For cross-namespace resources, label-based tracking is used instead of ownerReferences
                        Supports Go template syntax (e.g., "{{ .uid }}-namespace")
                      type: string
                    timeoutSeconds:
                      default: 300
                      description: |-
                        TimeoutSeconds is the maximum time to wait for the resource to be ready
                        Default: 300
                      format: int32
                      maximum: 3600
                      minimum: 1
                      type: integer
                    waitForReady:
                      default: true
                      description: |-
                        WaitForReady determines whether to wait for the resource to be ready before continuing
                        Default: true
                      type: boolean
                  required:
                  - id
                  - spec
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
              namespaces:
                description: |-
                  Namespaces defines Namespace resources to create
                  Note: Namespaces are cluster-scoped and always use label-based tracking
                  The targetNamespace field is ignored for Namespace resources
                items:
                  description: TResource defines a Kubernetes resource template with
                    policies and dependencies
                  properties:
                    annotationsTemplate:
                      additionalProperties:
                        type: string
                      description: AnnotationsTemplate defines annotations to apply
                        to the resource (supports templates)
                      type: object
                    conflictPolicy:
                      default: Stuck
                      description: |-
                        ConflictPolicy determines how to handle conflicts with existing resources
                        Default: Stuck (fail reconciliation if resource exists with different owner)
                      enum:
                      - Force
                      - Stuck
                      type: string
                    creationPolicy:
                      default: WhenNeeded
                      description: |-
                        CreationPolicy determines when the resource should be created
                        Default: WhenNeeded
                      enum:
                      - Once
                      - WhenNeeded
                      type: string
                    deletionPolicy:
                      default: Delete
                      description: |-
                        DeletionPolicy determines what happens to the resource when the LynqNode is deleted
                        Default: Delete
                      enum:
                      - Delete
                      - Retain
                      type: string
                    dependIds:
                      description: DependIds lists IDs of resources that must be ready
                        before this resource is created
                      items:
                        type: string
                      type: array
                    id:
                      description: ID is a unique identifier within the template (used
                        for dependencies and references)
                      type: string
                    ignoreFields:
                      description: |-
                        IgnoreFields specifies JSONPath expressions for fields to exclude from synchronization
                        These fields will be applied during initial creation but ignored in subsequent reconciliations
                        Only effective when CreationPolicy is WhenNeeded (default)
                        Allows fine-grained control for fields that should be managed externally (e.g., HPA-controlled replicas)
                        Example: ["$.spec.replicas", "$.spec.template.spec.containers[0].resources"]
                      items:
                        type: string
                      type: array
                    labelsTemplate:
                      additionalProperties:
                        type: string
                      description: LabelsTemplate defines labels to apply to the resource
                        (supports templates)
                      type: object
                    nameTemplate:
                      description: |-
                        NameTemplate is a Go template for the resource name
                        Template variables: .uid, .host, .hostOrUrl, and extraValueMappings
                      type: string
                    patchStrategy:
                      default: apply
                      description: |-
                        PatchStrategy determines how to apply the resource
                        Default: apply (Server-Side Apply)
                      enum:
                      - apply
                      - merge
                      - replace
                      type: string
                    skipOnDependencyFailure:
                      default: true
                      description: |-
                        SkipOnDependencyFailure determines whether to skip creating this resource when a dependency fails
                        When true (default): This resource will be skipped if any of its dependencies fail
                        When false: This resource will still be created even if dependencies fail (useful for cleanup resources)
                        Default: true
                      type: boolean
                    spec:
                      description: |-
                        Spec is the Kubernetes resource specification
                        Can be any Kubernetes native resource or custom resource
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    targetNamespace:
                      description: |-
                        TargetNamespace specifies the namespace where the resource should be created
                        If empty, defaults to the same namespace as the LynqNode CR
                        For cross-namespace resources, label-based tracking is used instead of ownerReferences
                        Supports Go template syntax (e.g., "{{ .uid }}-namespace")
                      type: string
                    timeoutSeconds:
                      default: 300
                      description: |-
                        TimeoutSeconds is the maximum time to wait for the resource to be ready
                        Default: 300
                      format: int32
                      maximum: 3600
                      minimum: 1
                      type: integer
                    waitForReady:
                      default: true
                      description: |-
                        WaitForReady determines whether to wait for the resource to be ready before continuing
                        Default: true
                      type: boolean
                  required:
                  - id
                  - spec
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
              networkPolicies:
                description: NetworkPolicies defines NetworkPolicy resources to create
                items:
                  description: TResource defines a Kubernetes resource template with
                    policies and dependencies
                  properties:
                    annotationsTemplate:
                      additionalProperties:
                        type: string
                      description: AnnotationsTemplate defines annotations to apply
                        to the resource (supports templates)
                      type: object
                    conflictPolicy:
                      default: Stuck
                      description: |-
                        ConflictPolicy determines how to handle conflicts with existing resources
                        Default: Stuck (fail reconciliation if resource exists with different owner)
                      enum:
                      - Force
                      - Stuck
                      type: string
                    creationPolicy:
                      default: WhenNeeded
                      description: |-
                        CreationPolicy determines when the resource should be created
                        Default: WhenNeeded
                      enum:
                      - Once
                      - WhenNeeded
                      type: string
                    deletionPolicy:
                      default: Delete
                      description: |-
                        DeletionPolicy determines what happens to the resource when the LynqNode is deleted
                        Default: Delete
                      enum:
                      - Delete
                      - Retain
                      type: string
                    dependIds:
                      description: DependIds lists IDs of resources that must be ready
                        before this resource is created
                      items:
                        type: string
                      type: array
                    id:
                      description: ID is a unique identifier within the template (used
                        for dependencies and references)
                      type: string
                    ignoreFields:
                      description: |-
                        IgnoreFields specifies JSONPath expressions for fields to exclude from synchronization
                        These fields will be applied during initial creation but ignored in subsequent reconciliations
                        Only effective when CreationPolicy is WhenNeeded (default)
                        Allows fine-grained control for fields that should be managed externally (e.g., HPA-controlled replicas)
                        Example: ["$.spec.replicas", "$.spec.template.spec.containers[0].resources"]
                      items:
                        type: string
                      type: array
                    labelsTemplate:
                      additionalProperties:
                        type: string
                      description: LabelsTemplate defines labels to apply to the resource
                        (supports templates)
                      type: object
                    nameTemplate:
                      description: |-
                        NameTemplate is a Go template for the resource name
                        Template variables: .uid, .host, .hostOrUrl, and extraValueMappings
                      type: string
                    patchStrategy:
                      default: apply
                      description: |-
                        PatchStrategy determines how to apply the resource
                        Default: apply (Server-Side Apply)
                      enum:
                      - apply
                      - merge
                      - replace
                      type: string
                    skipOnDependencyFailure:
                      default: true
                      description: |-
                        SkipOnDependencyFailure determines whether to skip creating this resource when a dependency fails
                        When true (default): This resource will be skipped if any of its dependencies fail
                        When false: This resource will still be created even if dependencies fail (useful for cleanup resources)
                        Default: true
                      type: boolean
                    spec:
                      description: |-
                        Spec is the Kubernetes resource specification
                        Can be any Kubernetes native resource or custom resource
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    targetNamespace:
                      description: |-
                        TargetNamespace specifies the namespace where the resource should be created
                        If empty, defaults to the same namespace as the LynqNode CR
                        For cross-namespace resources, label-based tracking is used instead of ownerReferences
                        Supports Go template syntax (e.g., "{{ .uid }}-namespace")
                      type: string
                    timeoutSeconds:
                      default: 300
                      description: |-
                        TimeoutSeconds is the maximum time to wait for the resource to be ready
                        Default: 300
                      format: int32
                      maximum: 3600
                      minimum: 1
                      type: integer
                    waitForReady:
                      default: true
                      description: |-
                        WaitForReady determines whether to wait for the resource to be ready before continuing
                        Default: true
                      type: boolean
                  required:
                  - id
                  - spec
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
              persistentVolumeClaims:
                description: PersistentVolumeClaims defines PVC resources to create
                items:
                  description: TResource defines a Kubernetes resource template with
                    policies and dependencies
                  properties:
                    annotationsTemplate:
                      additionalProperties:
                        type: string
                      description: AnnotationsTemplate defines annotations to apply
                        to the resource (supports templates)
                      type: object
                    conflictPolicy:
                      default: Stuck
                      description: |-
                        ConflictPolicy determines how to handle conflicts with existing resources
                        Default: Stuck (fail reconciliation if resource exists with different owner)
                      enum:
                      - Force
                      - Stuck
                      type: string
                    creationPolicy:
                      default: WhenNeeded
                      description: |-
                        CreationPolicy determines when the resource should be created
                        Default: WhenNeeded
                      enum:
                      - Once
                      - WhenNeeded
                      type: string
                    deletionPolicy:
                      default: Delete
                      description: |-
                        DeletionPolicy determines what happens to the resource when the LynqNode is deleted
                        Default: Delete
                      enum:
                      - Delete
                      - Retain
                      type: string
                    dependIds:
                      description: DependIds lists IDs of resources that must be ready
                        before this resource is created
                      items:
                        type: string
                      type: array
                    id:
                      description: ID is a unique identifier within the template (used
                        for dependencies and references)
                      type: string
                    ignoreFields:
                      description: |-
                        IgnoreFields specifies JSONPath expressions for fields to exclude from synchronization
                        These fields will be applied during initial creation but ignored in subsequent reconciliations
                        Only effective when CreationPolicy is WhenNeeded (default)
                        Allows fine-grained control for fields that should be managed externally (e.g., HPA-controlled replicas)
                        Example: ["$.spec.replicas", "$.spec.template.spec.containers[0].resources"]
                      items:
                        type: string
                      type: array
                    labelsTemplate:
                      additionalProperties:
                        type: string
                      description: LabelsTemplate defines labels to apply to the resource
                        (supports templates)
                      type: object
                    nameTemplate:
                      description: |-
                        NameTemplate is a Go template for the resource name
                        Template variables: .uid, .host, .hostOrUrl, and extraValueMappings
                      type: string
                    patchStrategy:
                      default: apply
                      description: |-
                        PatchStrategy determines how to apply the resource
                        Default: apply (Server-Side Apply)
                      enum:
                      - apply
                      - merge
                      - replace
                      type: string
                    skipOnDependencyFailure:
                      default: true
                      description: |-
                        SkipOnDependencyFailure determines whether to skip creating this resource when a dependency fails
                        When true (default): This resource will be skipped if any of its dependencies fail
                        When false: This resource will still be created even if dependencies fail (useful for cleanup resources)
                        Default: true
                      type: boolean
                    spec:
                      description: |-
                        Spec is the Kubernetes resource specification
                        Can be any Kubernetes native resource or custom resource
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    targetNamespace:
                      description: |-
                        TargetNamespace specifies the namespace where the resource should be created
                        If empty, defaults to the same namespace as the LynqNode CR
                        For cross-namespace resources, label-based tracking is used instead of ownerReferences
                        Supports Go template syntax (e.g., "{{ .uid }}-namespace")
                      type: string
                    timeoutSeconds:
                      default: 300
                      description: |-
                        TimeoutSeconds is the maximum time to wait for the resource to be ready
                        Default: 300
                      format: int32
                      maximum: 3600
                      minimum: 1
                      type: integer
                    waitForReady:
                      default: true
                      description: |-
                        WaitForReady determines whether to wait for the resource to be ready before continuing
                        Default: true
                      type: boolean
                  required:
                  - id
                  - spec
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
              podDisruptionBudgets:
                description: PodDisruptionBudgets defines PDB resources to create
                items:
                  description: TResource defines a Kubernetes resource template with
                    policies and dependencies
                  properties:
                    annotationsTemplate:
                      additionalProperties:
                        type: string
                      description: AnnotationsTemplate defines annotations to apply
                        to the resource (supports templates)
                      type: object
                    conflictPolicy:
                      default: Stuck
                      description: |-
                        ConflictPolicy determines how to handle conflicts with existing resources
                        Default: Stuck (fail reconciliation if resource exists with different owner)
                      enum:
                      - Force
                      - Stuck
                      type: string
                    creationPolicy:
                      default: WhenNeeded
                      description: |-
                        CreationPolicy determines when the resource should be created
                        Default: WhenNeeded
                      enum:
                      - Once
                      - WhenNeeded
                      type: string
                    deletionPolicy:
                      default: Delete
                      description: |-
                        DeletionPolicy determines what happens to the resource when the LynqNode is deleted
                        Default: Delete
                      enum:
                      - Delete
                      - Retain
                      type: string
                    dependIds:
                      description: DependIds lists IDs of resources that must be ready
                        before this resource is created
                      items:
                        type: string
                      type: array
                    id:
                      description: ID is a unique identifier within the template (used
                        for dependencies and references)
                      type: string
                    ignoreFields:
                      description: |-
                        IgnoreFields specifies JSONPath expressions for fields to exclude from synchronization
                        These fields will be applied during initial creation but ignored in subsequent reconciliations
                        Only effective when CreationPolicy is WhenNeeded (default)
                        Allows fine-grained control for fields that should be managed externally (e.g., HPA-controlled replicas)
                        Example: ["$.spec.replicas", "$.spec.template.spec.containers[0].resources"]
                      items:
                        type: string
                      type: array
                    labelsTemplate:
                      additionalProperties:
                        type: string
                      description: LabelsTemplate defines labels to apply to the resource
                        (supports templates)
                      type: object
                    nameTemplate:
                      description: |-
                        NameTemplate is a Go template for the resource name
                        Template variables: .uid, .host, .hostOrUrl, and extraValueMappings
                      type: string
                    patchStrategy:
                      default: apply
                      description: |-
                        PatchStrategy determines how to apply the resource
                        Default: apply (Server-Side Apply)
                      enum:
                      - apply
                      - merge
                      - replace
                      type: string
                    skipOnDependencyFailure:
                      default: true
                      description: |-
                        SkipOnDependencyFailure determines whether to skip creating this resource when a dependency fails
                        When true (default): This resource will be skipped if any of its dependencies fail
                        When false: This resource will still be created even if dependencies fail (useful for cleanup resources)
                        Default: true
                      type: boolean
                    spec:
                      description: |-
                        Spec is the Kubernetes resource specification
                        Can be any Kubernetes native resource or custom resource
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    targetNamespace:
                      description: |-
                        TargetNamespace specifies the namespace where the resource should be created
                        If empty, defaults to the same namespace as the LynqNode CR
                        For cross-namespace resources, label-based tracking is used instead of ownerReferences
                        Supports Go template syntax (e.g., "{{ .uid }}-namespace")
                      type: string
                    timeoutSeconds:
                      default: 300
                      description: |-
                        TimeoutSeconds is the maximum time to wait for the resource to be ready
                        Default: 300
                      format: int32
                      maximum: 3600
                      minimum: 1
                      type: integer
                    waitForReady:
                      default: true
                      description: |-
                        WaitForReady determines whether to wait for the resource to be ready before continuing
                        Default: true
                      type: boolean
                  required:
                  - id
                  - spec
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
              rollout:
                description: |-
                  Rollout defines the rollout strategy for LynqNode updates
                  When configured, node updates are throttled based on maxSkew to prevent
                  all nodes from updating simultaneously
                properties:
                  maxSkew:
                    default: 0
                    description: |-
                      MaxSkew is the maximum number of LynqNodes that can be updating simultaneously
                      0 (default) means unlimited - all nodes update at once (current behavior)
                      1 means serial rollout - one node at a time
                      N means up to N nodes can be updating in parallel
                      Each LynqForm applies maxSkew independently (template-isolated strategy)
                    format: int32
                    minimum: 0
                    type: integer
                  progressDeadlineSeconds:
                    default: 600
                    description: |-
                      ProgressDeadlineSeconds is the maximum time in seconds for a node update to become ready
                      before the rollout marks the node as failed
                      Default: 600 (10 minutes)
                    format: int32
                    maximum: 3600
                    minimum: 60
                    type: integer
                type: object
              secrets:
                description: Secrets defines Secret resources to create
                items:
                  description: TResource defines a Kubernetes resource template with
                    policies and dependencies
                  properties:
                    annotationsTemplate:
                      additionalProperties:
                        type: string
                      description: AnnotationsTemplate defines annotations to apply
                        to the resource (supports templates)
                      type: object
                    conflictPolicy:
                      default: Stuck
                      description: |-
                        ConflictPolicy determines how to handle conflicts with existing resources
                        Default: Stuck (fail reconciliation if resource exists with different owner)
                      enum:
                      - Force
                      - Stuck
                      type: string
                    creationPolicy:
                      default: WhenNeeded
                      description: |-
                        CreationPolicy determines when the resource should be created
                        Default: WhenNeeded
                      enum:
                      - Once
                      - WhenNeeded
                      type: string
                    deletionPolicy:
                      default: Delete
                      description: |-
                        DeletionPolicy determines what happens to the resource when the LynqNode is deleted
                        Default: Delete
                      enum:
                      - Delete
                      - Retain
                      type: string
                    dependIds:
                      description: DependIds lists IDs of resources that must be ready
                        before this resource is created
                      items:
                        type: string
                      type: array
                    id:
                      description: ID is a unique identifier within the template (used
                        for dependencies and references)
                      type: string
                    ignoreFields:
                      description: |-
                        IgnoreFields specifies JSONPath expressions for fields to exclude from synchronization
                        These fields will be applied during initial creation but ignored in subsequent reconciliations
                        Only effective when CreationPolicy is WhenNeeded (default)
                        Allows fine-grained control for fields that should be managed externally (e.g., HPA-controlled replicas)
                        Example: ["$.spec.replicas", "$.spec.template.spec.containers[0].resources"]
                      items:
                        type: string
                      type: array
                    labelsTemplate:
                      additionalProperties:
                        type: string
                      description: LabelsTemplate defines labels to apply to the resource
                        (supports templates)
                      type: object
                    nameTemplate:
                      description: |-
                        NameTemplate is a Go template for the resource name
                        Template variables: .uid, .host, .hostOrUrl, and extraValueMappings
                      type: string
                    patchStrategy:
                      default: apply
                      description: |-
                        PatchStrategy determines how to apply the resource
                        Default: apply (Server-Side Apply)
                      enum:
                      - apply
                      - merge
                      - replace
                      type: string
                    skipOnDependencyFailure:
                      default: true
                      description: |-
                        SkipOnDependencyFailure determines whether to skip creating this resource when a dependency fails
                        When true (default): This resource will be skipped if any of its dependencies fail
                        When false: This resource will still be created even if dependencies fail (useful for cleanup resources)
                        Default: true
                      type: boolean
                    spec:
                      description: |-
                        Spec is the Kubernetes resource specification
                        Can be any Kubernetes native resource or custom resource
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    targetNamespace:
                      description: |-
                        TargetNamespace specifies the namespace where the resource should be created
                        If empty, defaults to the same namespace as the LynqNode CR
                        For cross-namespace resources, label-based tracking is used instead of ownerReferences
                        Supports Go template syntax (e.g., "{{ .uid }}-namespace")
                      type: string
                    timeoutSeconds:
                      default: 300
                      description: |-
                        TimeoutSeconds is the maximum time to wait for the resource to be ready
                        Default: 300
                      format: int32
                      maximum: 3600
                      minimum: 1
                      type: integer
                    waitForReady:
                      default: true
                      description: |-
                        WaitForReady determines whether to wait for the resource to be ready before continuing
                        Default: true
                      type: boolean
                  required:
                  - id
                  - spec
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
              serviceAccounts:
                description: ServiceAccounts defines ServiceAccount resources to create
                items:
                  description: TResource defines a Kubernetes resource template with
                    policies and dependencies
                  properties:
                    annotationsTemplate:
                      additionalProperties:
                        type: string
                      description: AnnotationsTemplate defines annotations to apply
                        to the resource (supports templates)
                      type: object
                    conflictPolicy:
                      default: Stuck
                      description: |-
                        ConflictPolicy determines how to handle conflicts with existing resources
                        Default: Stuck (fail reconciliation if resource exists with different owner)
                      enum:
                      - Force
                      - Stuck
                      type: string
                    creationPolicy:
                      default: WhenNeeded
                      description: |-
                        CreationPolicy determines when the resource should be created
                        Default: WhenNeeded
                      enum:
                      - Once
                      - WhenNeeded
                      type: string
                    deletionPolicy:
                      default: Delete
                      description: |-
                        DeletionPolicy determines what happens to the resource when the LynqNode is deleted
                        Default: Delete
                      enum:
                      - Delete
                      - Retain
                      type: string
                    dependIds:
                      description: DependIds lists IDs of resources that must be ready
                        before this resource is created
                      items:
                        type: string
                      type: array
                    id:
                      description: ID is a unique identifier within the template (used
                        for dependencies and references)
                      type: string
                    ignoreFields:
                      description: |-
                        IgnoreFields specifies JSONPath expressions for fields to exclude from synchronization
                        These fields will be applied during initial creation but ignored in subsequent reconciliations
                        Only effective when CreationPolicy is WhenNeeded (default)
                        Allows fine-grained control for fields that should be managed externally (e.g., HPA-controlled replicas)
                        Example: ["$.spec.replicas", "$.spec.template.spec.containers[0].resources"]
                      items:
                        type: string
                      type: array
                    labelsTemplate:
                      additionalProperties:
                        type: string
                      description: LabelsTemplate defines labels to apply to the resource
                        (supports templates)
                      type: object
                    nameTemplate:
                      description: |-
                        NameTemplate is a Go template for the resource name
                        Template variables: .uid, .host, .hostOrUrl, and extraValueMappings
                      type: string
                    patchStrategy:
                      default: apply
                      description: |-
                        PatchStrategy determines how to apply the resource
                        Default: apply (Server-Side Apply)
                      enum:
                      - apply
                      - merge
                      - replace
                      type: string
                    skipOnDependencyFailure:
                      default: true
                      description: |-
                        SkipOnDependencyFailure determines whether to skip creating this resource when a dependency fails
                        When true (default): This resource will be skipped if any of its dependencies fail
                        When false: This resource will still be created even if dependencies fail (useful for cleanup resources)
                        Default: true
                      type: boolean
                    spec:
                      description: |-
                        Spec is the Kubernetes resource specification
                        Can be any Kubernetes native resource or custom resource
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    targetNamespace:
                      description: |-
                        TargetNamespace specifies the namespace where the resource should be created
                        If empty, defaults to the same namespace as the LynqNode CR
                        For cross-namespace resources, label-based tracking is used instead of ownerReferences
                        Supports Go template syntax (e.g., "{{ .uid }}-namespace")
                      type: string
                    timeoutSeconds:
                      default: 300
                      description: |-
                        TimeoutSeconds is the maximum time to wait for the resource to be ready
                        Default: 300
                      format: int32
                      maximum: 3600
                      minimum: 1
                      type: integer
                    waitForReady:
                      default: true
                      description: |-
                        WaitForReady determines whether to wait for the resource to be ready before continuing
                        Default: true
                      type: boolean
                  required:
                  - id
                  - spec
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
              services:
                description: Services defines Service resources to create
                items:
                  description: TResource defines a Kubernetes resource template with
                    policies and dependencies
                  properties:
                    annotationsTemplate:
                      additionalProperties:
                        type: string
                      description: AnnotationsTemplate defines annotations to apply
                        to the resource (supports templates)
                      type: object
                    conflictPolicy:
                      default: Stuck
                      description: |-
                        ConflictPolicy determines how to handle conflicts with existing resources
                        Default: Stuck (fail reconciliation if resource exists with different owner)
                      enum:
                      - Force
                      - Stuck
                      type: string
                    creationPolicy:
                      default: WhenNeeded
                      description: |-
                        CreationPolicy determines when the resource should be created
                        Default: WhenNeeded
                      enum:
                      - Once
                      - WhenNeeded
                      type: string
                    deletionPolicy:
                      default: Delete
                      description: |-
                        DeletionPolicy determines what happens to the resource when the LynqNode is deleted
                        Default: Delete
                      enum:
                      - Delete
                      - Retain
                      type: string
                    dependIds:
                      description: DependIds lists IDs of resources that must be ready
                        before this resource is created
                      items:
                        type: string
                      type: array
                    id:
                      description: ID is a unique identifier within the template (used
                        for dependencies and references)
                      type: string
                    ignoreFields:
                      description: |-
                        IgnoreFields specifies JSONPath expressions for fields to exclude from synchronization
                        These fields will be applied during initial creation but ignored in subsequent reconciliations
                        Only effective when CreationPolicy is WhenNeeded (default)
                        Allows fine-grained control for fields that should be managed externally (e.g., HPA-controlled replicas)
                        Example: ["$.spec.replicas", "$.spec.template.spec.containers[0].resources"]
                      items:
                        type: string
                      type: array
                    labelsTemplate:
                      additionalProperties:
                        type: string
                      description: LabelsTemplate defines labels to apply to the resource
                        (supports templates)
                      type: object
                    nameTemplate:
                      description: |-
                        NameTemplate is a Go template for the resource name
                        Template variables: .uid, .host, .hostOrUrl, and extraValueMappings
                      type: string
                    patchStrategy:
                      default: apply
                      description: |-
                        PatchStrategy determines how to apply the resource
                        Default: apply (Server-Side Apply)
                      enum:
                      - apply
                      - merge
                      - replace
                      type: string
                    skipOnDependencyFailure:
                      default: true
                      description: |-
                        SkipOnDependencyFailure determines whether to skip creating this resource when a dependency fails
                        When true (default): This resource will be skipped if any of its dependencies fail
                        When false: This resource will still be created even if dependencies fail (useful for cleanup resources)
                        Default: true
                      type: boolean
                    spec:
                      description: |-
                        Spec is the Kubernetes resource specification
                        Can be any Kubernetes native resource or custom resource
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    targetNamespace:
                      description: |-
                        TargetNamespace specifies the namespace where the resource should be created
                        If empty, defaults to the same namespace as the LynqNode CR
                        For cross-namespace resources, label-based tracking is used instead of ownerReferences
                        Supports Go template syntax (e.g., "{{ .uid }}-namespace")
                      type: string
                    timeoutSeconds:
                      default: 300
                      description: |-
                        TimeoutSeconds is the maximum time to wait for the resource to be ready
                        Default: 300
                      format: int32
                      maximum: 3600
                      minimum: 1
                      type: integer
                    waitForReady:
                      default: true
                      description: |-
                        WaitForReady determines whether to wait for the resource to be ready before continuing
                        Default: true
                      type: boolean
                  required:
                  - id
                  - spec
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
              statefulSets:
                description: StatefulSets defines StatefulSet resources to create
                items:
                  description: TResource defines a Kubernetes resource template with
                    policies and dependencies
                  properties:
                    annotationsTemplate:
                      additionalProperties:
                        type: string
                      description: AnnotationsTemplate defines annotations to apply
                        to the resource (supports templates)
                      type: object
                    conflictPolicy:
                      default: Stuck
                      description: |-
                        ConflictPolicy determines how to handle conflicts with existing resources
                        Default: Stuck (fail reconciliation if resource exists with different owner)
                      enum:
                      - Force
                      - Stuck
                      type: string
                    creationPolicy:
                      default: WhenNeeded
                      description: |-
                        CreationPolicy determines when the resource should be created
                        Default: WhenNeeded
                      enum:
                      - Once
                      - WhenNeeded
                      type: string
                    deletionPolicy:
                      default: Delete
                      description: |-
                        DeletionPolicy determines what happens to the resource when the LynqNode is deleted
                        Default: Delete
                      enum:
                      - Delete
                      - Retain
                      type: string
                    dependIds:
                      description: DependIds lists IDs of resources that must be ready
                        before this resource is created
                      items:
                        type: string
                      type: array
                    id:
                      description: ID is a unique identifier within the template (used
                        for dependencies and references)
                      type: string
                    ignoreFields:
                      description: |-
                        IgnoreFields specifies JSONPath expressions for fields to exclude from synchronization
                        These fields will be applied during initial creation but ignored in subsequent reconciliations
                        Only effective when CreationPolicy is WhenNeeded (default)
                        Allows fine-grained control for fields that should be managed externally (e.g., HPA-controlled replicas)
                        Example: ["$.spec.replicas", "$.spec.template.spec.containers[0].resources"]
                      items:
                        type: string
                      type: array
                    labelsTemplate:
                      additionalProperties:
                        type: string
                      description: LabelsTemplate defines labels to apply to the resource
                        (supports templates)
                      type: object
                    nameTemplate:
                      description: |-
                        NameTemplate is a Go template for the resource name
                        Template variables: .uid, .host, .hostOrUrl, and extraValueMappings
                      type: string
                    patchStrategy:
                      default: apply
                      description: |-
                        PatchStrategy determines how to apply the resource
                        Default: apply (Server-Side Apply)
                      enum:
                      - apply
                      - merge
                      - replace
                      type: string
                    skipOnDependencyFailure:
                      default: true
                      description: |-
                        SkipOnDependencyFailure determines whether to skip creating this resource when a dependency fails
                        When true (default): This resource will be skipped if any of its dependencies fail
                        When false: This resource will still be created even if dependencies fail (useful for cleanup resources)
                        Default: true
                      type: boolean
                    spec:
                      description: |-
                        Spec is the Kubernetes resource specification
                        Can be any Kubernetes native resource or custom resource
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    targetNamespace:
                      description: |-
                        TargetNamespace specifies the namespace where the resource should be created
                        If empty, defaults to the same namespace as the LynqNode CR
                        For cross-namespace resources, label-based tracking is used instead of ownerReferences
                        Supports Go template syntax (e.g., "{{ .uid }}-namespace")
                      type: string
                    timeoutSeconds:
                      default: 300
                      description: |-
                        TimeoutSeconds is the maximum time to wait for the resource to be ready
                        Default: 300
                      format: int32
                      maximum: 3600
                      minimum: 1
                      type: integer
                    waitForReady:
                      default: true
                      description: |-
                        WaitForReady determines whether to wait for the resource to be ready before continuing
                        Default: true
                      type: boolean
                  required:
                  - id
                  - spec
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - id
                x-kubernetes-list-type: map
            required:
            - hubId
            type: object
          status:
            description: LynqFormStatus defines the observed state of LynqForm.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the form's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the generation observed by the
                  controller
                format: int64
                type: integer
              readyNodes:
                description: ReadyNodes is the number of Ready LynqNodes using this
                  form
                format: int32
                type: integer
              rollout:
                description: Rollout tracks the progress of template rollouts when
                  rollout.maxSkew is configured
                properties:
                  completionTime:
                    description: CompletionTime is when the rollout completed (success
                      or failure)
                    format: date-time
                    type: string
                  message:
                    description: Message provides a human-readable status message
                    type: string
                  phase:
                    description: Phase is the current phase of the rollout
                    enum:
                    - Idle
                    - InProgress
                    - Failed
                    - Complete
                    type: string
                  readyUpdatedNodes:
                    description: ReadyUpdatedNodes is the number of nodes that are
                      updated AND Ready
                    format: int32
                    type: integer
                  startTime:
                    description: StartTime is when the rollout started
                    format: date-time
                    type: string
                  targetGeneration:
                    description: TargetGeneration is the LynqForm generation being
                      rolled out
                    format: int64
                    type: integer
                  totalNodes:
                    description: TotalNodes is the total number of LynqNodes using
                      this form
                    format: int32
                    type: integer
                  updatedNodes:
                    description: UpdatedNodes is the number of nodes that have been
                      updated to the target generation
                    format: int32
                    type: integer
                  updatingNodes:
                    description: UpdatingNodes is the number of nodes currently updating
                      (not Ready yet)
                    format: int32
                    type: integer
                type: object
              totalNodes:
                description: TotalNodes is the total number of LynqNodes using this
                  form
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
